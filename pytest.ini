[pytest]
testpaths = tests
# кеш не пишем: tests/ смонтирована в контейнер только на чтение
addopts = -p no:cacheprovider -m "not live"
markers =
    live: ходит в настоящую сеть (docker compose run --rm test -m live)
